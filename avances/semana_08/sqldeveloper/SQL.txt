CREATE TABLE "CLIENTE" (
	"ID_CLIENTE"	INTEGER,
	"NOMBRE"	TEXT NOT NULL CHECK(LENGTH("NOMBRE") <= 100),
	"CORREO_ELECTRONICO"	TEXT NOT NULL CHECK(LENGTH("CORREO_ELECTRONICO") <= 100),
	"TELEFONO"	TEXT CHECK(LENGTH("TELEFONO") <= 20),
	PRIMARY KEY("ID_CLIENTE" AUTOINCREMENT)
);

CREATE TABLE "MASCOTA" (
	"N_CHIP"	INTEGER,
	"NOMBRE_MASCOTA"	TEXT NOT NULL CHECK(LENGTH("NOMBRE_MASCOTA") <= 25),
	"ESPECIE"	TEXT NOT NULL CHECK(LENGTH("ESPECIE") <= 30),
	"RAZA"	TEXT NOT NULL CHECK(LENGTH("RAZA") <= 30),
	"PESO"	REAL NOT NULL,
	"FECHA_NACIMIENTO"	DATE NOT NULL,
	"SEXO"	TEXT NOT NULL CHECK(LENGTH("SEXO") = 1),
	"ID_CLIENTE"	INTEGER NOT NULL,
	PRIMARY KEY("N_CHIP"),
	FOREIGN KEY("ID_CLIENTE") REFERENCES "CLIENTE"("ID_CLIENTE")
);

CREATE TABLE "CONSULTA_DETALLADA" (
	"N_CONSULTA"	INTEGER,
	"MOTIVO_CONSULTA"	TEXT NOT NULL CHECK(LENGTH("MOTIVO_CONSULTA") <= 150),
	"EXAMEN_AUXILIAR"	TEXT NOT NULL CHECK(LENGTH("EXAMEN_AUXILIAR") <= 100),
	"TRATAMIENTO"	TEXT NOT NULL CHECK(LENGTH("TRATAMIENTO") <= 100),
	"DETALLES_EXTRAS"	TEXT NOT NULL CHECK(LENGTH("DETALLES_EXTRAS") <= 200),
	"FECHA"	DATE NOT NULL,
	"N_CHIP"	INTEGER NOT NULL,
	PRIMARY KEY("N_CONSULTA" AUTOINCREMENT),
	FOREIGN KEY("N_CHIP") REFERENCES "MASCOTA"("N_CHIP")
);